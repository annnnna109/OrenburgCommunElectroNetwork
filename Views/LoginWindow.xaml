<Window x:Class="OrenburgCommunElectroNetwork.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:OrenburgCommunElectroNetwork.Converters"
        Title="Вход в систему - АО 'Оренбургкоммунэлектросеть'"
        Height="500" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.Resources>
        <local:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibilityConverter"/>
        <local:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
        </Style>

        <!-- Упрощенный стиль для PasswordBox -->
        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinHeight" Value="34"/>
            <!-- Минимальная высота для выравнивания -->
        </Style>
    </Window.Resources>

    <Grid Background="#F8F9FA" Margin="0,0,0,-122">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="9*"/>
            <ColumnDefinition Width="191*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#2C3E50" Padding="20" Grid.ColumnSpan="2">
            <StackPanel HorizontalAlignment="Center">
                <TextBlock Text="🔐" FontSize="28" HorizontalAlignment="Center"/>
                <TextBlock Text="Вход в систему" 
                           Foreground="White" 
                           FontSize="18" 
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           Margin="0,5,0,0"/>
                <TextBlock Text="АО 'Оренбургкоммунэлектросеть'" 
                           Foreground="#BDC3C7"
                           FontSize="12"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Форма логина -->
        <Border Grid.Row="1" Background="White" Margin="12,30,30,30" Padding="30" CornerRadius="5" Grid.Column="1">
            <StackPanel>
                <!-- Логин -->
                <TextBlock Text="Логин:" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="UsernameBox" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource TextBoxStyle}"
                         Margin="0,0,0,15"/>

                <!-- Пароль -->
                <TextBlock Text="Пароль:" FontWeight="Bold" Margin="0,0,0,5"/>
                <PasswordBox x:Name="PasswordBox" 
                             PasswordChanged="PasswordBox_PasswordChanged"
                             Style="{StaticResource PasswordBoxStyle}"
                             Margin="0,0,0,20"/>

                <!-- Запомнить меня -->
                <CheckBox Content="Запомнить меня" 
                          IsChecked="{Binding RememberMe}"
                          Margin="0,0,0,20"/>

                <!-- Сообщение об ошибке -->
                <Border Background="#FFEBEE" 
                        BorderBrush="#EF5350" 
                        BorderThickness="1" 
                        CornerRadius="3" 
                        Padding="10"
                        Visibility="{Binding HasError, Converter={StaticResource BoolToVisibilityConverter}}"
                        Margin="0,0,0,15">
                    <TextBlock Text="{Binding ErrorMessage}" 
                               Foreground="#D32F2F" 
                               TextWrapping="Wrap"/>
                </Border>

                <!-- Кнопки -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <!-- Кнопка Вход -->
                    <Button Command="{Binding LoginCommand}"
                            Width="120">
                        <Button.Style>
                            <Style TargetType="Button" BasedOn="{StaticResource LoginButtonStyle}">
                                <Setter Property="Background" Value="#27AE60"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter Property="Content" Value="Вход"/>
                                <Setter Property="IsEnabled" Value="{Binding IsLoggingIn, Converter={StaticResource InverseBoolToVisibilityConverter}}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsLoggingIn}" Value="True">
                                        <Setter Property="Content" Value="Вход..."/>
                                        <Setter Property="Background" Value="#95A5A6"/>
                                        <Setter Property="IsEnabled" Value="False"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Кнопка Выход -->
                    <Button Content="Выход" 
                            Command="{Binding CloseCommand}"
                            Style="{StaticResource LoginButtonStyle}"
                            Background="#E74C3C" 
                            Foreground="White"
                            Width="120"
                            Margin="15,0,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Подвал -->
        <Border Grid.Row="1" Background="#ECF0F1" Padding="10" Grid.ColumnSpan="2" Margin="0,454,0,0">
            <TextBlock Text="© 2025 АО 'Оренбургкоммунэлектросеть'. Все права защищены." 
                       Foreground="#7F8C8D"
                       FontSize="11"
                       HorizontalAlignment="Center" Margin="0,159,0,0"/>
        </Border>
    </Grid>
</Window>